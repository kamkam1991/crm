{% extends "CrmBundle::main.html.twig" %}
{% set subMenu= [
    {'path':'reguest_list', 'name': 'Lista zapotrzebowań'},
    {'path':'reguest_add', 'name': 'Dodaj zapotrzebowanie'},
    {'path':'company_update', 'name': 'Dane firmy'},
] 
%}
{% block nav %}
<div class="container-fluid">
    <div class="row">
        <div class="col-sm-2 col-md-2 col-lf-2 sidebar">
            <ul class="nav nav-sidebar">
                {% for item in subMenu %}
                    <li class="{{ app.request.get('_route') == item['path'] ? 'active': ''}}"><a href="{{ path(item['path']) }}">{{ item['name'] }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>      
{% endblock nav %}
    
{% block pageContainer %}

{% endblock pageContainer %} 